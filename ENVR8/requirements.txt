# ENVR8 Advanced Platform Requirements
# Python Dependencies
mlflow>=2.8.0
kubeflow>=1.8.0
apache-airflow>=2.7.0
prefect>=2.14.0
snyk>=0.9.0

# Security Tools
# Install via system package manager:
# Ubuntu: sudo apt-get install snort wazuh-manager osquery
# Mac: brew install snort wazuh osquery
# Windows: choco install snort

# Monitoring Stack
prometheus-client>=0.17.0
grafana-api>=1.0.3
elasticsearch>=8.11.0

# Development Tools
black>=23.0.0
pytest>=7.4.0
pytest-cov>=4.1.0
bandit>=1.7.5

# Node.js Dependencies (package.json will be created separately)
# React, Vite, Next.js, ESLint, Prettier

# Cross-Platform Support
docker>=6.1.0
kubernetes>=26.1.0
ansible>=8.0.0
terraform>=1.5.0
